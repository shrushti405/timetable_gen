# timetable_gen
# run guii.py file to see the timetable gui and inputs you can add input via loading timetable config file so you dont have to manually type inputs 
#welcome page -> load configuration -> timetable_config3.json (inputs will automatically added)
